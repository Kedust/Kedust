const config = {
    api: {
        domain: "https://api.kedust.be",
        menu: {
            getAll: "/Menu/GetAll",
            get: "/Menu"
        },
        choice:{
            getByMenu: "/Choice/GetByMenu"
        }
    }
};

export default config;